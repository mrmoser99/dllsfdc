 List<cllease__Calendar__c> buckets = new List<cllease__Calendar__c>();

		Integer year = date.today().year();

		String search = '%' + string.valueOf(year);
        buckets = [select name from cllease__Calendar__c where name like :search];